<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:elevation="10dp">
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_marginLeft="16dp"
        android:layout_toLeftOf="@+id/favchck"
        android:id="@+id/txt_win_add"/>
    <ImageView
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentRight="true"
        android:layout_margin="16dp"
        android:src="@drawable/blankheart"
        android:id="@+id/favchck" />
    <Spinner
        android:layout_width="fill_parent"
        android:layout_height="50dp"
        android:layout_below="@+id/txt_win_add"
        android:visibility="gone"
        android:layout_margin="16dp"
        android:id="@+id/category_items"
        >
    </Spinner>
    <Button
        android:id="@+id/submit_selected_item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorAccent"
        android:visibility="gone"
        android:layout_margin="16dp"
        android:layout_below="@+id/category_items"
        android:text="submit"
        android:textColor="#fff" />
</RelativeLayout>
